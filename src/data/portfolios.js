// Legacy prototype portfolio time series (mock).
//
// EMPTIED during backend integration. Real portfolio data is served by the
// engine at /api/clients/:id/portfolio and /api/clients/:id/snapshots.
// Kept as an empty export only so legacy pages that import it still build.

export const portfolios = {}
