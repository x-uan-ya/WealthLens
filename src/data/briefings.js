// Legacy prototype meeting briefings (Sarah/Catherine-era mock).
//
// EMPTIED during backend integration. RM briefs are now generated per client
// by the grounded AI endpoint (/api/clients/:id/brief) and reviewed in the
// Intelligence Detail workflow. No fabricated briefing figures are served here.
// Kept as an empty export only so the legacy Briefings page still builds.

export const briefings = []
