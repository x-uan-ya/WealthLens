// Legacy prototype client book (Sarah/Catherine-era mock).
//
// EMPTIED during backend integration. The official 20-client book is served by
// the engine at /api/clients and consumed via dataService's getOfficialClients().
// No fabricated client figures are served here. Kept as an empty export only so
// legacy pages that still import it build.

export const clients = []
